# s21_cat and s21_grep - Утилиты командной строки

Две реализации стандартных Unix утилит на языке C для учебного проекта.

## 📁 Структура проекта

```
src/
├── s21_cat/           # Реализация утилиты cat
│   ├── s21_cat.c
│   ├── s21_cat.h
│   ├── Makefile
│   └── tests.sh
├── s21_grep/          # Реализация утилиты grep
│   ├── s21_grep.c
│   ├── s21_grep.h
│   ├── Makefile
│   └── tests.sh
└── README.md          # Общая документация
```

## 📋 Общее описание

Этот проект содержит реализации двух стандартных Unix утилит:
- **s21_cat** - утилита для вывода содержимого файлов
- **s21_grep** - утилита для поиска текста по шаблону

Проект разработан в рамках учебной программы для изучения системного программирования на языке C.

---

## 🐱 s21_cat

### Компиляция и запуск
```bash
cd src/s21_cat
make           # Компиляция
./s21_cat      # Запуск
make clean     # Очистка
```

### Основные флаги
| Флаг | Длинная версия | Описание |
|------|----------------|----------|
| `-b` | `--number-nonblank` | Нумеровать только непустые строки |
| `-e` | | Показать `$` в конце строк и непечатаемые символы |
| `-E` | `--show-ends` | Показать `$` в конце строк |
| `-n` | `--number` | Нумеровать все строки |
| `-s` | `--squeeze-blank` | Сжимать несколько пустых строк в одну |
| `-t` | | Показать табы как `^I` и непечатаемые символы |
| `-T` | `--show-tabs` | Показать табы как `^I` |
| `-v` | | Показать непечатаемые символы |

### Примеры использования
```bash
# Простой вывод
./s21_cat file.txt

# С нумерацией строк
./s21_cat -n file.txt

# Показать концы строк
./s21_cat -E file.txt

# Комбинирование флагов
./s21_cat -n -E -T file1.txt file2.txt
```

---

## 🔍 s21_grep

### Компиляция и запуск
```bash
cd src/s21_grep
make           # Компиляция
./s21_grep     # Запуск
make clean     # Очистка
```

### Основные флаги
| Флаг | Описание |
|------|----------|
| `-e` | Шаблон для поиска |
| `-i` | Игнорировать регистр |
| `-v` | Инвертировать поиск (показать несоответствующие) |
| `-c` | Только количество совпадений |
| `-l` | Только имена файлов с совпадениями |
| `-n` | Показать номера строк |
| `-h` | Скрыть имена файлов |
| `-s` | Подавить сообщения об ошибках |
| `-f` | Читать шаблоны из файла |
| `-o` | Показать только совпадающую часть |

### Примеры использования
```bash
# Простой поиск
./s21_grep pattern file.txt

# Поиск с игнорированием регистра
./s21_grep -i PATTERN file.txt

# Поиск с номерами строк
./s21_grep -n pattern file.txt

# Поиск в нескольких файлах
./s21_grep pattern file1.txt file2.txt

# Использование шаблонов из файла
./s21_grep -f patterns.txt file.txt
```

---

## 🧪 Тестирование

Обе утилиты имеют скрипты для автоматического тестирования:

### Тестирование s21_cat
```bash
cd src/s21_cat
chmod +x tests.sh
./tests.sh
```

### Тестирование s21_grep
```bash
cd src/s21_grep
chmod +x tests.sh
./tests.sh
```

Тестовые скрипты сравнивают вывод реализованных утилит с выводом стандартных Unix команд `cat` и `grep`.

---

## 🛠️ Технические детали

### Общие характеристики
- **Язык**: C11 (стандарт C11)
- **Компилятор**: gcc с флагами `-Wall -Wextra -Werror`
- **Библиотеки**: Только стандартная библиотека C
- **Система сборки**: Make

### Особенности реализации

**s21_cat:**
- Поддержка длинных опций (GNU-стиль)
- Корректная обработка непечатаемых символов
- Сжатие пустых строк
- Совместимость с оригинальной утилитой cat

**s21_grep:**
- Поддержка регулярных выражений (POSIX Extended)
- Чтение шаблонов из файла
- Обработка множественных флагов
- Оптимизированный поиск по шаблонам

---

## 📚 Дополнительные проекты

В репозитории также содержатся другие учебные проекты:

- **game_of_life** - Игра "Жизнь" Конвея с ASCII-графикой
- **pong** - Классическая игра Pong в терминале
- **math_plotter** - Построитель графиков математических функций
- **ai_module_manager** - Система управления базой данных модулей ИИ

---

## 👨‍💻 Автор

**Кашапов Нияз** — студент Школы 21 (Казань)  
GitHub: [millember](https://github.com/millember)

---

*Учебный проект по языку C и системному программированию. Все реализации разработаны с нуля для изучения работы стандартных Unix утилит.*
